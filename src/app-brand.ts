export const APP_NAME = 'HomeGrid';

/** Primary job export extension. */
export const JOB_FILE_EXT = 'homegrid';

/** Legacy extension; import still accepts these files. */
export const LEGACY_JOB_FILE_EXT = 'wirer';

export const PREFERENCE_KEY_PREFIX = 'homegrid:';
